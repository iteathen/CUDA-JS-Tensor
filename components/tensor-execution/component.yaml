{
  "schema_version": 1,
  "component": {
    "id": "tensor.execution",
    "name": "Resolved tensor plan and dense execution",
    "status": "public-alpha",
    "product_area": "components",
    "path": "components/tensor-execution",
    "purpose": "Bind immutable tensor plans to complete deterministic host-planned execution or compile one statically item-independent plan as a copied typed Device-JS leaf library, with explicit finite resources and ownership.",
    "public_contracts": ["index.mjs", "index.d.ts"],
    "allowed_dependencies": ["node-standard-library", "tensor.value", "tensor.program-plan", "public-cuda-js-0.1.0-alpha.16"],
    "forbidden_dependencies": ["cuda-js-internals", "native-cuda", "embedded-cuda-source", "consumer-policy", "neural-policy", "mcgs-policy", "the_restaurant"],
    "validation": ["npm run test:tensor-execution", "npm run test:package", "npm run smoke:native:tensor-execution", "npm run verify"],
    "owner": "tensor.resolved-plan/tensor.backend.simt/tensor.backend.fusion/tensor.backend.cublaslt/tensor.backend.device-callable/tensor.execution-result",
    "lifecycle": "Resolved plans own fixed library plans plus compiled module/function/prepared-DAG capabilities and share one session/runtime adapter lease; each result owns only run-created tensors/views/workspaces; copied TensorDeviceProgram records own no live runtime resource.",
    "design": {
      "owned_invariant": "One accepted mathematical TensorPlan resolves to one finite inspectable host-planned identity or one statically independent item-callable identity without changing mathematics, hiding resources or duplicating consumer scheduling.",
      "public_ports": ["ResolvedTensorPlan", "resolveTensorPlan", "TensorExecutionResult", "TensorDeviceProgram", "compileTensorDeviceProgram"],
      "injected_dependencies": ["TensorSession", "TensorPlan or TensorProgram", "public CUDA-JS runtime from the session"],
      "adapter_boundaries": ["generated Device-JS program/library compiler", "public CUDA-JS cuBLASLt plan", "public CUDA-JS prepared operation DAG", "public CUDA-JS typed leaf-library composition"],
      "simplest_sufficient_total_system": "One mathematical plan with separately removable host-planned and item-callable realizations, one CUDA-JS boundary, and no Tensor-owned scheduler or native escape."
    }
  }
}
